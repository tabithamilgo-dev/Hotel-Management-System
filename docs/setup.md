#Setup documentation
The project is organized into different modules:

- 'users/' → handles authentication and user profiles
- 'users/' → manages hotel rooms
- 'reservations/' → booking and reservation system
- 'payments' → payment tracking
- 'templates' → HTML templates
-'statics/' → CSS, JS, images
